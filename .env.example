# Copy to .env and fill in. .env is gitignored; this file is not.
#
# Phase 1 needs no keys — embeddings run locally via sentence-transformers
# and retrieval runs against a local Chroma directory.
#
# Phase 2 adds the generation step, which does need a key:
# ANTHROPIC_API_KEY=
